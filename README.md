---
title: ClinicOps API
emoji: 🏥
colorFrom: blue
colorTo: green
sdk: docker
app_port: 7860
---

# ClinicOps API

Minimum deployable surface. `GET /health` only — enough to prove the deployment pipe
works end to end before any real code is written.

Build steps live in `specs/step-00.md` through `specs/step-11.md`. Design docs are in
`docs/`. Project rules are in `CLAUDE.md`.
